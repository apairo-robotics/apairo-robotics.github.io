<section class="hero">
  <div class="wrap hero-grid">
    <div class="hero-copy">
      <p class="eyebrow" data-reveal>Robotics sensor-data stack</p>

      <h1 data-reveal style="--delay: 60ms">
        From raw bags to training-ready datasets.
      </h1>

      <p class="lede" data-reveal style="--delay: 120ms">
        <strong>apairo</strong> is an open, modular Python toolkit for robotics sensor data.
        Profile any dataset in one command, then load, sync, transform and visualise
        LiDAR, images and trajectories through one numpy-native API.
      </p>

      <div class="hero-actions" data-reveal style="--delay: 180ms">
        <a class="btn" href="https://apairo-robotics.github.io/apairo/" target="_blank" rel="noopener">
          Read the docs
        </a>
        <a class="btn btn--ghost" href="https://github.com/apairo-robotics/apairo" target="_blank" rel="noopener">
          View on GitHub
        </a>
      </div>

      <div class="install" data-reveal style="--delay: 240ms">
        <code class="mono"><span class="install-prompt">$</span> pip install apairo</code>
        <button class="install-copy" data-copy="pip install apairo" aria-label="Copy install command">Copy</button>
      </div>

      <p class="hero-meta" data-reveal style="--delay: 280ms">
        v0.8.0 · MIT licence · Python ≥ 3.11 ·
        <a href="#cite">Cite</a>
      </p>
    </div>

    <figure class="hero-fig" data-reveal style="--delay: 120ms">
      <img
        src="/assets/hero-rellis.webp"
        width="1006"
        height="655"
        alt="Top-down view of a forest trail reconstructed from LiDAR scans, with the robot's trajectory drawn along the path."
        fetchpriority="high"
      />
      <figcaption>
        RELLIS-3D, sequence 00000: 80 LiDAR scans placed with their poses.
        The robot's trajectory is drawn in ochre.
      </figcaption>
    </figure>
  </div>

  <hr class="rule" />
</section>

<style>
  .hero { padding-top: clamp(3rem, 7vw, 5rem); }
  .hero-grid {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: clamp(2rem, 5vw, 4rem);
    align-items: center;
    padding-bottom: clamp(3rem, 7vw, 5rem);
  }

  h1 {
    font-size: clamp(2.3rem, 4.4vw, 3.5rem);
    line-height: 1.08;
    margin-top: 1.2rem;
    max-width: 16ch;
  }

  .lede {
    margin-top: 1.5rem;
    font-size: clamp(1.02rem, 1.4vw, 1.15rem);
    color: var(--ink-2);
    max-width: 48ch;
  }
  .lede strong { font-weight: 600; }

  .hero-actions {
    margin-top: 2rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
  }

  .install {
    margin-top: 1.5rem;
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    background: var(--paper-soft);
    border: 1px solid var(--line);
    border-radius: 8px;
    padding: 0.5rem 0.55rem 0.5rem 0.9rem;
    font-size: 0.88rem;
  }
  .install code { color: var(--ink); white-space: nowrap; }
  .install-prompt { color: var(--steel); }
  .install-copy {
    font: inherit;
    font-size: 0.78rem;
    font-weight: 500;
    color: var(--steel);
    background: transparent;
    border: 1px solid var(--line);
    border-radius: 6px;
    padding: 0.25em 0.65em;
    cursor: pointer;
    transition: color 0.2s var(--ease), border-color 0.2s var(--ease);
  }
  .install-copy:hover { color: var(--ink); border-color: var(--line-strong); }

  .hero-meta {
    margin-top: 1.2rem;
    font-size: 0.85rem;
    color: var(--steel);
  }
  .hero-meta a {
    color: var(--ink-2);
    border-bottom: 1px solid var(--line-strong);
  }
  .hero-meta a:hover { color: var(--ink); border-color: var(--ink); }

  .hero-fig { margin: 0; }
  .hero-fig img { display: block; width: 100%; height: auto; }
  .hero-fig figcaption {
    margin-top: 0.4rem;
    font-size: 0.8rem;
    line-height: 1.5;
    color: var(--steel);
    text-align: center;
  }

  @media (max-width: 900px) {
    .hero-grid { grid-template-columns: 1fr; }
    .hero-fig { max-width: 520px; }
  }
</style>
